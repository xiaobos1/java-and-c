int main()
{
	int a = 10000;
	FILE* pf = fopen("lian.txt", "wb");
	if (pf == NULL)
	{
		perror("fopen");
		return 1;
	}
	//写文件
	fwrite(&a, sizeof(int), 1, pf);

	//关闭
	fclose(pf);
	pf = NULL;
	return 0;
}